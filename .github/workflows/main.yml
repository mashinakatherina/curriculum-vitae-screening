name: Main Workflow
on: [workflow_dispatch]
jobs:
  LoadDataset:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repo content
        uses: actions/checkout@v2 # checkout the repository content
      - run: pip install kaggle
      - run: ls
      - run: python ./src/data_loading/copy_config_file.py
